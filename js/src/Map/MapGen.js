class MapGeneration {
  constructor (x, y) {
    this.originX = x;
    this.originY = y;
  }
  generate (map) {

  }
}

class Terrain extends MapGeneration {
  constructor (x, y) {
    super(x, y);
  }
  generate (map) {

  }
}

class Structure extends MapGeneration {
  constructor (x, y) {
    super(x, y);
  }
  generate (map) {
    
  }
}